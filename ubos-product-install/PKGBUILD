developer="http://indiecomputing.com/"
url="http://ubos.net/"
maintainer=$developer
pkgname=$(basename $(pwd))
pkgver=0.1
pkgrel=1
pkgdesc="Installs a product configuration after boot"
arch=('any')
license=('AGPL3')
depends=('ubos-install')
options=('!strip')

package() {
# Code
    install -D -m0755 ${startdir}/bin/ubos-product-install -t ${pkgdir}/usr/bin/

# Where product definitions are found
    mkdir -p ${pkgdir}/etc/${pkgname}

# Systemd
    install -D -m0644 ${startdir}/systemd/ubos-product-install.service -t ${pkgdir}/usr/lib/systemd/system/
}
